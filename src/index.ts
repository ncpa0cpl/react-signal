import {
  createSignal,
  useCreateSignal,
  useOnReceive,
  type Signal,
} from "./signal";

export { createSignal, useOnReceive, useCreateSignal, type Signal };
export default {
  createSignal,
  useOnReceive,
  useCreateSignal,
};
